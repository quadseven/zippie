<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Info.plist for the home-screen widget extension (issue #244).

     HAND WRITTEN, same reason as the tunnel extension's: NSExtension is a
     nested dictionary and the INFOPLIST_KEY_* build settings that drive
     GENERATE_INFOPLIST_FILE carry flat values only. project.yml deliberately
     carries NO `info:` key for this target - that key tells xcodegen to WRITE
     this file itself, which the first attempt at this target proved by
     silently dropping the NSExtension dictionary below.

     NO NSExtensionPrincipalClass, unlike the tunnel. WidgetKit's extension
     point is newer than NetworkExtension's and looks up its entry point from
     the `@main` attribute on the WidgetBundle instead - naming a principal
     class here would be for an API this extension does not use. -->
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleDisplayName</key>
    <string>Zippie</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>NSExtension</key>
    <dict>
        <key>NSExtensionPointIdentifier</key>
        <string>com.apple.widgetkit-extension</string>
    </dict>
</dict>
</plist>
